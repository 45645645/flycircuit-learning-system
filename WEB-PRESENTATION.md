# FlyCircuit 網頁呈現規範

本文件定義 FlyCircuit 知識庫的網頁呈現與 UI 品質工作。目標是讓小型網頁問題可由專責代理人獨立接手，使主要研發代理人持續專注於 Python、napari、資料分析、影像流程與後續教材。

## 工作範圍

可以修改：

- 排版、留白、字級、行長與視覺層級。
- 桌面及手機響應式設計。
- 圖片與動畫的顯示尺寸、比例、載入穩定性及放大互動。
- 導覽、焦點狀態、鍵盤操作與無障礙語意。
- 共用樣式 `assets/style.css`、共用互動 `assets/app.js`，以及 `scripts/build.py` 內的 HTML 結構。

不得修改：

- Python 資料分析、napari、影像演算法及其產出邏輯。
- 統計數字、科學解讀、論文證據、知識節點或原始 `.am` 資料。
- 公開／私密素材範圍。
- GitHub Pages 或其他外部發布設定；不得發布網站。
- `site/` 內的生成檔；所有頁面必須由 `scripts/build.py` 建置。

## 視覺方向

- 乾淨、現代、克制的學術型教材；以內容理解優先，不仿製其他網站。
- 神經元影像維持黑底灰階。driver line 顏色只用於標籤、細線與邊框。
- 正文維持舒適行長與清楚層級；卡片和提示框不應搶過研究內容。
- 所有媒體保持原始比例並完整顯示，不使用裁切填滿。
- 圖片與動畫必須提供可點擊、可鍵盤操作的放大檢視。

## 神經影像教材媒體規則

- 850px 以上使用雙欄：動畫欄約 35%，三視圖欄約 65%。
- 850px 以下改為單欄，依序呈現動畫與三視圖。
- 動畫不應成為頁面最大的視覺元素：桌面高度上限 360px；手機顯示寬度上限 260px，置中呈現。
- 三視圖可使用欄位完整寬度，但必須維持比例，不可裁切。
- `img` 必須提供正確 `width`、`height`、替代文字及 lazy loading，以預留版面並避免載入跳動。
- 動畫與圖片放大後必須能以 Escape 關閉、關閉按鈕操作，並將焦點返回觸發元素。
- 尊重 `prefers-reduced-motion`；UI 動畫不得妨礙閱讀。

## 手機版要求

- 以 390px 寬度為主要驗證尺寸；不得產生整頁水平捲動。
- 導覽可在自身範圍橫向捲動，但不得撐寬頁面。
- 文字、按鈕及媒體不可貼齊螢幕邊緣；觸控目標需清楚可辨。
- 摘要數字、卡片和圖說保持可讀，不因壓縮造成重疊或截斷。
- 放大圖片不得超出可視範圍，背景捲動應在檢視期間鎖定。

## 工作流程與驗證

1. 閱讀 `AGENTS.md` 與本文件，確認工作不涉及科學內容。
2. 檢查來源檔、生成方式與既有變更；只做小型、可逆的修改。
3. 執行 `python3 scripts/build.py` 建置本機網站。
4. 在瀏覽器檢查 1440px、1024px、390px 的版面與媒體比例。
5. 驗證圖片放大、鍵盤焦點、Escape、手機導覽及版面溢出。
6. 執行：

   ```text
   python3 scripts/build.py
   python3 scripts/check.py --allow-missing-sources
   ```

7. 檢查 console error／warning 及全站內部連結。
8. 回報修改檔案、測試結果、限制與仍不確定之處。
